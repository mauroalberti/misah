# Tests on Linux at every push, and a crate upload that never happens on its
# own. This is still the primary repository; it has simply stopped being the
# place the Python distribution is built.
#
# That moved to GitHub on 2026-09-08, where this repository is push-mirrored,
# for one reason: macOS and Windows runners, free and unmetered on public
# repositories, on the two platforms most QGIS installations run. Until then
# neither had a wheel, and the reason was never the code -- the Apple SDK
# licence question is about extracting an SDK to build from Linux, and does not
# arise on Apple hardware; `rusqlite`'s bundled feature wants a C compiler, not
# LLVM. Both were problems of *cross*-compilation, and a native runner dissolves
# them.
#
# The Linux wheels and the sdist went with them, and not for tidiness. Two
# pipelines able to upload the same version to PyPI is a hazard and not a
# redundancy: a version is final once uploaded, and the second attempt would
# fail at best. One place builds every artifact, one place uploads them. The
# reasoning that used to fill this file now lives in `.github/workflows/CI.yml`
# -- including why the compatibility floor is a decision rather than an
# accident of an image.
#
# What stays here is what GitHub does not do: every Rust-side check, the Python
# suites on Linux, and `cargo publish`. The crate goes to crates.io from here
# and from nowhere else.
#
# `publish:crates` is `when: manual`. crates.io does not let a version be
# replaced once uploaded, so the tag pipeline runs the tests and then waits for
# someone to press the button, rather than making `git push --tags` the
# irreversible act.

default:
  # The latest stable 1.x on Debian bookworm. Floating within the major version
  # rather than pinned: this is a small crate on edition 2018/2021 with no
  # nightly features, so a stable release breaking it is itself worth knowing
  # about, and a pin would only defer finding out.
  image: rust:1-bookworm
  # A superseded pipeline has nothing left to say, so let a new push cancel it.
  interruptible: true

variables:
  # GitLab caches only what sits inside the build directory, and cargo's home
  # does not, hence the move.
  CARGO_HOME: $CI_PROJECT_DIR/.cargo
  CARGO_TERM_COLOR: always

# One pipeline per change, not two: without this, a branch that has an open
# merge request runs both a branch pipeline and a merge-request one.
workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    # Tags, or `publish:crates` could never run: on a tag pipeline
    # `$CI_COMMIT_BRANCH` is empty, so without this none of the rules below
    # match and no pipeline is created at all.
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never
    - if: $CI_COMMIT_BRANCH

cache:
  # Keyed on the lockfile: dependencies change only when it does, and a stale
  # key would have every job recompile the whole tree.
  key:
    files:
      - Cargo.lock
  paths:
    - .cargo/registry/index
    - .cargo/registry/cache
    - target/

stages:
  - test
  - publish

# The kernel crate, which is where every test lives and which depends on no
# interpreter. Kept apart from the workspace build for that reason: whatever
# happens to the Python side, this job still says whether the geometry is
# right.
test:kernel:
  stage: test
  script:
    - rustc --version && cargo --version
    # --locked: build the dependency versions Cargo.lock records, and fail
    # rather than quietly resolve new ones, so a green pipeline means the
    # committed lockfile is the one that was tested.
    - cargo test -p misah --locked
    # The examples are how the kernels are run against the reference datasets,
    # so they are worth compiling even though nothing imports them.
    - cargo build -p misah --examples --locked

# pyo3 wants an interpreter to configure the build, even for an abi3 extension
# that deliberately links none of it. Anything touching pylib needs this first.
.with-python:
  before_script:
    - apt-get update -qq && apt-get install -y -qq --no-install-recommends python3

# The whole workspace, pylib included, so that a break in the bindings does not
# wait for a release to show itself.
build:workspace:
  extends: .with-python
  stage: test
  script:
    - cargo build --workspace --locked

# Clippy over everything, tests and examples included, denying warnings.
#
# It earns being a gate: it was clippy that caught ten assertion messages
# written as `"...{err}"` in an edition-2018 crate, where a lone literal is not
# a format string, so each would have printed the placeholder instead of the
# error at the moment a test failed in this very pipeline.
#
# rustfmt is deliberately not here. The spacing in this tree is a house style,
# and `cargo fmt --check` would fail on the first file it opened; making it a
# gate would mean reformatting the crate, which is a decision of its own and not
# a side effect of wanting a linter.
lint:clippy:
  extends: .with-python
  stage: test
  script:
    # The official Rust image installs the minimal profile, which leaves clippy
    # out; a no-op when some future image ships it.
    - rustup component add clippy
    - cargo clippy --workspace --all-targets --locked -- -D warnings

# The Python bindings, actually imported and called rather than merely compiled.
#
# `build:workspace` proves the extension builds; it says nothing about whether
# `import misah` finds it, whether array shapes and index handling survive the
# boundary, or whether a pyo3 signature default resolves. Those are separate
# claims, and this suite has already caught one of them.
#
# All seven suites run here, the raster one included. `test_kernels.py` reads
# the Malpi crop from `example_data` and cuts out of it the 200 x 247 window
# geoSurfDEM computed the golden trace on: the two crops sit on one grid, seven
# cells apart in each direction, so the wider one committed here contains the
# narrower value for value and the expected vertex counts carry over unchanged.
# What used to need a second repository on the machine now needs this checkout
# and nothing else.
test:python:
  stage: test
  before_script:
    # PEP 668 marks the Debian interpreter externally managed, so pip needs a
    # virtualenv even here.
    - apt-get update -qq
    - apt-get install -y -qq --no-install-recommends python3 python3-pip python3-venv
    - python3 -m venv /tmp/venv
    - . /tmp/venv/bin/activate && pip install -q maturin numpy
  script:
    - . /tmp/venv/bin/activate
    # The built wheel rather than `maturin develop`: it is what a user installs,
    # and installing it is itself part of what this job is checking.
    - cd pylib && maturin build --release --out /tmp/wheels && cd ..
    - . /tmp/venv/bin/activate && pip install --no-index /tmp/wheels/*.whl
    # From the repository root, never from pylib, whose source tree shadows the
    # installed package.
    - python3 pylib/tests/test_stress.py
    - python3 pylib/tests/test_mesh.py
    - python3 pylib/tests/test_inversion.py
    - python3 pylib/tests/test_best_fit.py
    - python3 pylib/tests/test_kernels.py
    - python3 pylib/tests/test_fields.py
    - python3 pylib/tests/test_mechanisms.py

# ---------------------------------------------------------------------------
# Publishing
# ---------------------------------------------------------------------------
#
# One job now, and it is manual, for the reason the header gives.
#
# PyPI used to be published from here too, through Trusted Publishing on a
# `release` environment, alongside the wheel jobs that fed it. Both went to
# GitHub together: artifacts and upload belong in one place, or a release
# becomes two buttons in two systems for one version. The trusted publisher
# registered on PyPI for this GitLab project is now unused, and can be removed
# from the project's Publishing settings -- leaving it is a standing permission
# for a pipeline that no longer intends to use it.
#
# The two were independent anyway: the Python distribution is `misah` on PyPI,
# the crate is `misah` on crates.io, and one can go out without the other.

# crates.io, which takes the kernel crate alone. `misah-py` is not published
# there: it is the extension, it depends on `misah` by path rather than by
# version, and nobody adds it to a Cargo.toml.
#
# Needs `CARGO_REGISTRY_TOKEN` as a masked, protected CI variable, and a
# protected tag to go with it. `--locked` so the published crate is built from
# the lockfile the tests ran against.
publish:crates:
  stage: publish
  rules:
    - if: $CI_COMMIT_TAG
      when: manual
  needs:
    - test:kernel
  script:
    - cargo publish -p misah --locked
